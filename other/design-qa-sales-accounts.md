# 销售账款页视觉核对

- 参考图：`/var/folders/bl/fvkqtvk95f74jl7bs8cl3g540000gn/T/codex-clipboard-6f4438f0-6dbf-4775-a21e-470e695de13d.png`
- 实现预览：`http://127.0.0.1:4175/sales-reconciliation.html?view=accounts`
- 核对视口：1280 × 720（本地浏览器预览）
- 核对方式：浏览器渲染结果 + 无障碍树，未执行大规模测试

## 已核对

- 筛选区包含发货日期、客户名称、食堂、上级单位、查询和重置；默认日期为 `2026-08-19` 至 `2026-09-19`。
- 账款表显示两条客户/食堂汇总记录，当前页合计和所有页合计均为 `554.00`，抹零金额为 `0.00`。
- 联系人格式与参考图一致：`-- (--)`、`李成志 (18515500000)`。
- 导出入口、每页条数、上一页/下一页禁用态、当前页和跳页信息已补齐。
- 选择“经费食堂”并查询后，结果正确收敛为 1 条、`486.00`；刷新后已恢复默认展示。
- 控制台告警/错误检查为空，目标脚本 `node --check` 通过。

## 结论

passed

参考图使用的是更宽桌面视口；在当前 1280px 预览中表格保留横向滚动以承载联系人列，筛选日期已增加窄屏自适应宽度。
