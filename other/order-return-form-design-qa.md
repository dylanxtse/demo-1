# 添加退货页面 Design QA

## 对比材料

- 源视觉：`/var/folders/bl/fvkqtvk95f74jl7bs8cl3g540000gn/T/codex-clipboard-26286d6d-50ec-4228-bfac-e372fced230d.png`
- 实现截图：Codex In-app Browser 本地预览 tab 5 的渲染截图（实现页面为 `http://127.0.0.1:4174/order-return-form.html?mode=add`）
- 源像素：2048 × 1198；实现预览视口：1280 × 720
- 对比状态：添加退货单，退货方式为“关联订单”，退货原因输入框聚焦并展开五项原因下拉菜单。
- 说明：由于当前桌面 In-app Browser 的固定预览视口与源图像素尺寸不同，本次按相同页面状态对结构锚点、布局节奏和控件比例进行复核，不将浏览器密度差异判定为视觉问题。

## 检查结果

- 页面保留企业端公共壳层，退货页表单区域改为截图中的三列基础字段布局。
- 退货方式默认显示“关联订单”；客户、食堂使用下拉选择控件；退货原因改为可输入并可展开的五项原因下拉菜单。
- 空商品区展示 9 行交替底色占位行、商品图片占位、数量/价格/金额/报损数量输入框和“合计”行。
- 表头补充申请退货数量帮助标识；表格支持截图所需的横向内容宽度和滚动区域。
- 补充订单备注及 `0/100` 字数提示，底部操作区调整为截图中的取消/保存双蓝按钮。
- 退货选择订单、保存、编辑、审核和原因选择逻辑保留；订单备注字数同步显示。

## 轻量交互复核

- 点击退货原因输入框：下拉菜单展开，五个选项可见。
- 点击“质量问题”：输入框写入对应文本，下拉菜单收起。
- 刷新页面：初始态仍显示 9 行占位商品、备注区和底部固定操作区。
- 工程检查：`node --check assets/js/pages/order-return-form.js` 通过；`git diff --check` 通过；未执行大规模测试。

final result: passed

## 整单/部分退演示商品数据迭代

- 演示订单补齐为多商品明细，包含商品名称、计量单位、下单单价、下单数量、发货数量和已退数量等字段。
- 已发货演示订单的整单退会回显全部未退商品及可退数量；部分退会回显同一订单的全部商品，但数量字段保持空白供填写。
- 净菜加工模块的单商品专用模拟订单不作为退货入口，避免用不完整的专项演示数据干扰退货流程。
- 轻量交互复核：可退订单首条整单退回填 4 个商品及数量；部分退回填 4 个商品且申请退货数量、发货数量和已退数保持空白。
- 工程检查：`node --check assets/js/utils/storage.js`、`node --check assets/js/pages/order-return-form.js`、`git diff --check` 通过；未执行大规模测试。

final result: passed

## 未选商品图片占位迭代

- 未选择商品时，退货明细的图片单元格保持空白，不再提前显示商品图片占位块。
- 选择商品后仍使用明细行的通用图片占位样式，保持已选商品状态的展示一致性。
- 轻量视觉复核：关联订单初始空明细行的图片列为空白，表格行高和其他字段布局不变。
- 工程检查：未执行大规模测试。

final result: passed

## 客户/食堂下拉占位项迭代

- “请选择客户”和“请选择食堂”改为 `selected + disabled + hidden` 的占位项，打开下拉列表时不作为可选数据展示。
- 选择客户或食堂后的回显、清空和必填校验逻辑保持不变。
- 轻量交互复核：客户下拉的占位文本仍正常显示，列表项仅保留可选客户；食堂使用同一规则。
- 工程检查：未执行大规模测试。

final result: passed

## 期望送达日期区间控件样式复用迭代

- 选择订单弹窗中的期望送达日期改为两个通用 `date-input-control` 日期输入控件，各自复用 `date-range-icon` 日历图标样式，中间保留区间分隔符。
- 开始日期和结束日期仍使用原有筛选字段及查询逻辑，日期选择行为不变。
- 轻量视觉复核：弹窗内两端输入框宽度均衡、图标位置一致，符合截图中的通用日期区间控件形态。
- 工程检查：`node --check assets/js/pages/order-return-form.js`、`git diff --check` 通过；未执行大规模测试。

final result: passed

## 不关联订单自定义添加商品迭代

- 入口状态：选择“不关联订单”后，选择客户和食堂，商品区显示“添加商品”按钮；客户或食堂未选择完整时仅提示先补充基础信息。
- 商品选择：弹窗复用项目商品选择视觉规范，支持商品名称/编号、商品分类筛选，当前页全选、多选、分页、重置和确认添加。
- 回填结果：确认后商品写入退货明细，自动带出商品名称、计量单位和市场价；申请退货数量、退货单价、报损数量和备注仍可继续编辑，重复商品不会再次添加。
- 模式切换：切回“关联订单”时清空无关联商品并恢复“选择订单”入口；无关联商品未填写申请退货数量时保存校验仍会提示补充商品数量。
- 轻量交互复核：已验证“不关联订单 → 第一实验学校 → 第一食堂 → 添加商品 → 选择土豆丝 → 添加”，明细成功出现；填写申请退货数量 2 后金额更新为 9.60；切回“关联订单”恢复订单选择按钮和空占位行。
- 工程检查：`node --check assets/js/pages/order-return-form.js`、`git diff --check` 通过；未执行大规模测试。

final result: passed

## 整单退 / 部分退回填迭代

- 整单退：按订单行计算 `发货数量 - 已退数量`，过滤掉可退数量为 0 的商品，回显全部剩余可退商品及数量。
- 部分退：回显订单商品行，发货数、已退数、申请退货数和报损数保持空白，等待用户手工填写。
- 边界状态：整单退时若订单没有可退商品，关闭选择弹窗并 toast 提示“订单已整单退”，不再渲染空商品列表。
- 轻量交互复核：已通过订单 `DD202607290200012` 验证整单退回显 2 个剩余商品及 30、40 数量；部分退回显 3 个商品且数量字段为空。
- 工程检查：`node --check assets/js/pages/order-return-form.js` 与 `git diff --check` 通过，未执行大规模测试。

final result: passed

## 无可退商品 toast 提示迭代

- 整单退选择到没有可退商品的订单时，页面显示 toast “订单已整单退”。
- 原退货明细保持不变，不进入“该订单暂无可退商品”的空列表状态；正常整单退与部分退回填逻辑不变。
- 工程检查：`node --check assets/js/pages/order-return-form.js` 与 `git diff --check` 通过，未执行大规模测试。

final result: passed

## 退货方式默认空值迭代

- 添加退货单初始不再默认选择“关联订单”或“不关联订单”，下拉框显示占位文字“请选择退货方式”。
- 只有手动选择退货方式后，才显示对应的选择订单或添加商品操作入口；必填校验仍提示选择退货方式。
- 编辑、审核和通过订单入口仍按已有单据或入口参数回显实际退货方式。
- 工程检查：`node --check assets/js/pages/order-return-form.js` 与 `git diff --check` 通过，未执行大规模测试。

final result: passed

## 关联订单选择弹窗迭代

- 源视觉：`/var/folders/bl/fvkqtvk95f74jl7bs8cl3g540000gn/T/codex-clipboard-72fc2db3-f492-4570-baf1-46953828ccf9.png`
- 实现页面：`http://127.0.0.1:4174/order-return-form.html?mode=add`，通过页面中的“选择订单”按钮打开弹窗。
- 核对状态：退货方式为“关联订单”，选择订单弹窗已打开。
- 核对内容：页面背景有半透明遮罩；弹窗包含标题、关闭按钮、订单号/商品名称/期望送达日期筛选、查询/重置、订单表格、整单退/部分退操作、结果数、分页和关闭按钮。
- 轻量交互：点击“选择订单”打开弹窗；点击查询/重置更新筛选；分页控件可切换；点击整单退或部分退回填订单并关闭弹窗；关闭按钮收起弹窗。
- 工程检查：仅执行 `node --check assets/js/pages/order-return-form.js` 与 `git diff --check`，未执行大规模测试。

final result: passed
