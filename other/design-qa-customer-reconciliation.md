# 客户对账设计核对

- 最终结果：`passed`
- 参考设计：用户提供的客户对账截图（3840×2234）
- 实现页面：`sales-reconciliation.html` 的“客户对账”页签
- 核对方式：本地浏览器预览 + DOM 结构核对 + 轻量交互检查

## 已核对

- 页面页签包含“明细对账 / 客户对账 / 对账单生成记录”，客户对账状态正确高亮。
- 已补充自定义查询、全部/未对账/部分对账/未结算/部分结算/已结算状态筛选。
- 已补充客户名称、食堂、业务日期范围、查询、重置和导出入口。
- 已补充客户编码、客户名称、食堂、联系人（电话）、对账状态、对账单笔数、对账金额、对账抹零、应收金额、已收金额、未收金额、操作等13列。
- 已补充8条截图对应的演示数据、状态颜色、金额格式、操作按钮和分页信息。
- 默认入口不带参数时，点击“客户对账”可以正常切换；选择“未对账”后显示4条对应记录。
- `sales-reconciliation-data.js` 与 `sales-reconciliation.js` 已通过 `node --check` 语法检查。

## 备注

窄窗口预览下表格保留横向滚动，以保证13列信息不被压缩；在截图对应的宽屏布局下会完整呈现表格列。
