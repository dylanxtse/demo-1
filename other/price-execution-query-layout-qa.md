# 价格执行清单查询区核对

## 核对范围

- 采购价执行清单 Tab
- 销售价执行清单 Tab
- 两个 Tab 的筛选控件宽度、占位文字字号、查询/重置按钮避让关系

## 修复结果

- 筛选网格改为自适应三列，查询操作区独立保留，不再覆盖商品名称输入框。
- 大屏下文本筛选控件统一以 304px 为上限，短枚举控件按参考图收窄；窄屏会随列宽缩小。
- 两个 Tab 的输入框和下拉占位文字统一为 14px，并沿用项目灰色文字变量。
- Tab 切换、查询、重置和导出按钮仍保持可用。

## 轻量验证

- 已在本地浏览器分别打开采购价执行清单、销售价执行清单进行视觉核对。
- 已确认 1280 宽度下查询按钮不再与商品名称输入框重叠。
- 已通过页面脚本语法检查和 `git diff --check`。

final result: passed
