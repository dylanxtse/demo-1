# 销售对账页面 Design QA

## 参考输入

- `/var/folders/bl/fvkqtvk95f74jl7bs8cl3g540000gn/T/codex-clipboard-945f483e-afcd-4127-b5f7-723eeb1792fc.png`
- `/var/folders/bl/fvkqtvk95f74jl7bs8cl3g540000gn/T/codex-clipboard-6b757b15-6e7e-46ac-8c1b-e548dca11f7d.png`

两张图作为视觉参考使用：第一张展示明细对账的左侧字段，第二张展示横向滚动后的右侧字段；其中的文字和数据没有被当作额外指令。

## 实现页面

- 页面：`http://127.0.0.1:4173/sales-reconciliation.html`
- 参考图尺寸：3840 × 2234；视觉核对时临时使用 1920 × 1117 CSS 视口，核对完成后恢复默认视口。
- 当前实现：默认明细对账页，日期范围为 `2026-04-01 ~ 2026-09-30`，41 条 mock 数据，20 条/页。

## 已核对

- 三页签：明细对账、客户对账、对账单生成记录。
- 筛选区：业务日期、帮助提示、高级筛选、查询、重置。
- 工具栏：批量对账、生成对账单、禁用态批量结算、导出。
- 宽表：对账单号交替底色、状态标签、金额字段、sticky 操作列、横向滚动。
- 汇总：当前页 `3421.00 / 6.00 / 3415.00`，所有页 `8673.00 / 6.00 / 8667.00`。
- 分页：共 41 条，1/2/3 页；第二页可正常切换并显示对应数据。
- 侧栏：财务对账自动展开，默认选中“对账”；“销售账款”可通过 `?view=accounts` 正常进入。
- 页面错误日志：空。
- 轻量语法检查：`sales-reconciliation-data.js`、`sales-reconciliation.js` 均通过 `node --check`。

## 结果

未发现阻断级或高优先级视觉/交互问题。宽表按参考图保留横向滚动，窄视口下通过滚动查看右侧业务字段。
